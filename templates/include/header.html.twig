<header>
    <div class="header">
        <h1 class="logo"><a href="{{ path('homepage') }}"><i class="fas fa-home"></i> Espoir sur Pattes <i class="fas fa-paw"></i></a><a
                class="button"><i class="fas fa-bars"></i></a></h1>

        <div class="flashmessage">
            {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
            {% endfor %}
        </div>

        <div id="responsive-nav" class="closeMenu">
            <div class="nav links">
                <nav>
                    <li><a href="">Carte</a></li>
                    <li><a href="">Blog</a></li>
                    <li><a href="">Contact</a></li>
                    <li><a href="">Associations</a></li>
                </nav>
            </div>
            <div class="nav login">
                {% if app.user == false %}
                <nav>
                    <li><a href="{{ path('app_login') }}">Connexion</a></li>
                    <li><a href="{{ path('create_account') }}">Inscription</a></li>
                </nav>
                {% endif %}
                 {% if app.user %}
                <nav>
                    <li><a href="">Bienvenue {{app.user.username|capitalize}}</a></li>
                    <li><a href="{{ path('app_logout') }}" onclick="return confirm('Confirmez la déconnexion ?')"><i class="fas fa-sign-out-alt"></i> Déconnexion</a></li>
                </nav>
                {% endif %}

            </div>
        </div>
    </div>
</header>

<body>
    <main>